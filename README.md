# rho: R Hydrology Optics

An R package with function to solve forward and inverse radiative transfer problems in aquatic environments.

### Install from Github:
```
# install.packages(remotes)
remotes::install_github("AlexCast/rho")
```


